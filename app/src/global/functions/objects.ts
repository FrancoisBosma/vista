// export const isObjectEmpty = (obj: Object) => Object.keys(obj).length === 0
// export const objectMap = (obj: Object, fn: Function, valueAsKey = false) =>
//   Object.fromEntries(Object.entries(obj).map(([k, v], i) => [!valueAsKey ? k : v, fn(v, k, i)]))
